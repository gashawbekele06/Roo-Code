active_intents:
  - id: "INT-001"
    name: "Build Weather API"
    status: "IN_PROGRESS"
    owned_scope:
      - "src/api/weather/**"
    constraints:
      - "Use open APIs only"
    acceptance_criteria:
      - "Tests pass"
    intent_language: "AISpec v1"  # Reference
    formal_definition: "Mathematical intent: f(x) = weather_api(x) where x in owned_scope"

  - id: "INT-002"
    name: "Refactor Auth"
    status: "PENDING"
    owned_scope:
      - "src/auth/**"
    constraints:
      - "Backward compatible"
    acceptance_criteria:
      - "No breaking changes"
    intent_language: "AISpec v1"  # Reference
    formal_definition: "Mathematical intent: f(x) = refactor(auth) where x in scope"